{
  "name": "colorshower-workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run build --workspace @moyarich/vscode-visualize-css-colors && npm run dev --workspace @moyarich/colorshower-playground",
    "build": "npm run build --workspace @moyarich/vscode-visualize-css-colors && npm run build --workspace @moyarich/colorshower-playground",
    "typecheck": "npm run typecheck --workspace @moyarich/vscode-visualize-css-colors && npm run typecheck --workspace @moyarich/colorshower-playground",
    "test": "npm run build --workspace @moyarich/vscode-visualize-css-colors && node --test tests/*.test.mjs",
    "package": "npm run package --workspace @moyarich/vscode-visualize-css-colors",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run lint && npm run format:check && npm run typecheck && npm test && npm run build && npm run package"
  },
  "engines": {
    "node": "^22.13.0 || >=24"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/moyarich/colorshower.git"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.11.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.12.0",
    "prettier": "^3.9.8",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.70.0"
  }
}
